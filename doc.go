/*
Package bitarray implements arrays with single bit booleans.

Compared to an array of built-in booleans, this saves memory (about eight times cheaper)
but reading and setting values is much slower (roughly ten times slower).
*/
package bitarray
